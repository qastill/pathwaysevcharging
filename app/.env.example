# Optional. Leave empty to run fully on-device (localStorage) — the default.
# When both are set, bookings, reviews and host listings sync to Supabase (see supabase/schema.sql).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
